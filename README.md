Este proyecto está desarrollado en Java y utiliza una base de datos MySQL.

📌 Base de datos

El archivo aputs_db.sql contiene toda la estructura y datos necesarios para que el proyecto funcione correctamente.
Debe importarse en XAMPP (o cualquier servidor SQL compatible con phpMyAdmin).

📁 Estructura del proyecto

En la carpeta src se encuentran las clases y paquetes principales del proyecto, incluyendo la lógica y conexión con la base de datos.

🔧 Requisitos

XAMPP u otro servidor SQL con phpMyAdmin

Java instalado y configurado
